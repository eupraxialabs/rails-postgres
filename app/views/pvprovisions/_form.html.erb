<%= form_for(pvprovision) do |f| %>
  <% if pvprovision.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pvprovision.errors.count, "error") %> prohibited this pvprovision from being saved:</h2>

      <ul>
      <% pvprovision.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
