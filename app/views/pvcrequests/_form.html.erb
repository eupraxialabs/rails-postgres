<%= form_for(pvcrequest) do |f| %>
  <% if pvcrequest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pvcrequest.errors.count, "error") %> prohibited this pvcrequest from being saved:</h2>

      <ul>
      <% pvcrequest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
